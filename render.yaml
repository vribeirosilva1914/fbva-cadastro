services:
  - type: web
    name: fbva-sistema
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:application -w 2 --timeout 120 -b 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: fbva-db
          property: connectionString
      - key: HTTPS
        value: "1"
      - key: FLASK_DEBUG
        value: "0"
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false

databases:
  - name: fbva-db
    plan: free
    databaseName: fbva
    user: fbva_user
